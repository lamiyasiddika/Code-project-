#include <stdio.h>
#include <string.h>
int main(){
    char s[1000]; fgets(s,1000,stdin);
    s[strcspn(s,"\n")]=0;
    int i=0,j=strlen(s)-1;
    while(i<j){ char t=s[i]; s[i]=s[j]; s[j]=t; i++; j--; }
    printf("%s\n", s); return 0;
}
