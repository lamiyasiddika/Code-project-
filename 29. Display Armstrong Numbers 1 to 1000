#include <stdio.h>
#include <math.h>

int main() {
    for (int n=1;n<=1000;n++) {
        int temp=n, sum=0;
        while(temp) {
            int r=temp%10;
            sum += r*r*r;
            temp/=10;
        }
        if(sum==n) printf("%d ",n);
    }
    return 0;
}
