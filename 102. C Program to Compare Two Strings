#include <stdio.h>
#include <string.h>
int main(){
    char a[1000], b[1000]; fgets(a,1000,stdin); fgets(b,1000,stdin);
    a[strcspn(a,"\n")]=0; b[strcspn(b,"\n")]=0;
    int cmp = strcmp(a,b);
    if(cmp==0) printf("Equal\n");
    else if(cmp<0) printf("First is smaller\n");
    else printf("First is greater\n");
    return 0;
}
