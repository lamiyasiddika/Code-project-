#include <stdio.h>
#include <string.h>
int main(){
    char s[2000]; fgets(s,2000,stdin); s[strcspn(s,"\n")]=0;
    char delim[50]; fgets(delim,50,stdin); delim[strcspn(delim,"\n")]=0;
    char *p = strtok(s, delim);
    while(p){ printf("%s\n", p); p = strtok(NULL, delim); }
    return 0;
}
