#include <stdio.h>
int main(){
    int r,c; scanf("%d %d",&r,&c);
    int a[r][c]; for(int i=0;i<r;i++) for(int j=0;j<c;j++) scanf("%d",&a[i][j]);
    if(r==1){ for(int j=0;j<c;j++) printf("%d ",a[0][j]); }
    else if(c==1){ for(int i=0;i<r;i++) printf("%d ",a[i][0]); }
    else{
        for(int j=0;j<c;j++) printf("%d ",a[0][j]);
        for(int i=1;i<r-1;i++) printf("%d ", a[i][c-1]);
        for(int j=c-1;j>=0;j--) printf("%d ", a[r-1][j]);
        for(int i=r-2;i>0;i--) printf("%d ", a[i][0]);
    }
    printf("\n"); return 0;
}
