#include <stdio.h>
#include <math.h>
int main(){
    int n; scanf("%d",&n);
    double sumsq=0; int a;
    long long trace=0;
    for(int i=0;i<n;i++) for(int j=0;j<n;j++){
        scanf("%d",&a);
        if(i==j) trace+=a;
        sumsq += (double)a*a;
    }
    printf("Trace=%lld\nFrobenius norm=%.6f\n", trace, sqrt(sumsq));
    return 0;
}
