#include <stdio.h>
#include <string.h>
int main(){
    char s[]="madam"; char *l=s, *r=s+strlen(s)-1;
    int ok=1;
    while(l<r){ if(*l!=*r){ ok=0; break;} l++; r--; }
    puts(ok?"Palindrome":"Not palindrome"); return 0;
}
