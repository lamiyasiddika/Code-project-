#include <stdio.h>

int main() {
    int a,b;
    scanf("%d %d",&a,&b);

    for (int n=a;n<=b;n++) {
        int temp=n, sum=0;
        while(temp){
            int r=temp%10;
            sum+=r*r*r;
            temp/=10;
        }
        if(sum==n) printf("%d ", n);
    }

    return 0;
}
