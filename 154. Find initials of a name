#include <stdio.h>
#include <string.h>
int main(){
    char name[]="Lamia Siddika"; printf("%c", name[0]);
    for(int i=0;i<strlen(name);i++) if(name[i]==' ' && name[i+1]) printf("%c", name[i+1]);
    putchar('\n'); return 0;
}
