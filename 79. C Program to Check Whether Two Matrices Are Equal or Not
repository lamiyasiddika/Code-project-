#include <stdio.h>
int main(){
    int r,c; scanf("%d %d",&r,&c);
    int a[r][c], b[r][c];
    for(int i=0;i<r;i++) for(int j=0;j<c;j++) scanf("%d",&a[i][j]);
    for(int i=0;i<r;i++) for(int j=0;j<c;j++) scanf("%d",&b[i][j]);
    int eq=1;
    for(int i=0;i<r && eq;i++) for(int j=0;j<c;j++) if(a[i][j]!=b[i][j]) { eq=0; break; }
    printf(eq? "Equal\n":"Not equal\n");
    return 0;
}
